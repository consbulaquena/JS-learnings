//alternative to "let", we use const
// Const is used, if we do not Re-Assign. Used Once only
// const is for code readability, value never change

const isRaining = true

//isRaining = false         Const is Used Once

console.log(isRaining)
